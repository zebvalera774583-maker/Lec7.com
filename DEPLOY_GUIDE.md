# 🚀 Руководство по деплою Lec7

## Варианты деплоя

### 1. Автоматический деплой через GitHub Actions (Рекомендуется) ⚡

**Как работает:**
- При каждом `git push` в ветку `main` автоматически запускается деплой
- Или можно запустить вручную через GitHub UI: **Actions** → **Deploy to Timeweb** → **Run workflow**

**Требования:**
1. Настроены секреты в GitHub:
   - `TIMEWEB_HOST` - IP адрес сервера (например: `194.87.104.179`)
   - `TIMEWEB_USER` - пользователь SSH (обычно `root`)
   - `TIMEWEB_SSH_KEY` - приватный SSH ключ для доступа к серверу

2. На сервере должен быть настроен SSH доступ

**Как настроить секреты:**
1. Откройте репозиторий на GitHub
2. Перейдите в **Settings** → **Secrets and variables** → **Actions**
3. Добавьте три секрета (см. выше)

**Использование:**
```bash
# Просто пушите код - деплой запустится автоматически
git add .
git commit -m "Новая фича"
git push
```

---

### 2. Локальный деплой через терминал (для быстрого деплоя)

#### Linux/Mac (Bash)

```bash
# Установите переменные окружения
export TIMEWEB_HOST="your-server-ip"
export TIMEWEB_USER="root"
export TIMEWEB_SSH_KEY="~/.ssh/your-key"

# Запустите деплой
./scripts/deploy-local.sh
```

#### Windows (PowerShell)

```powershell
# Установите переменные окружения
$env:TIMEWEB_HOST = "your-server-ip"
$env:TIMEWEB_USER = "root"
$env:TIMEWEB_SSH_KEY = "C:\Users\YourUser\.ssh\your-key"

# Запустите деплой
.\scripts\deploy-local.ps1
```

Или передайте параметры напрямую:
```powershell
.\scripts\deploy-local.ps1 -Host "your-ip" -User "root" -SshKey "path\to\key"
```

---

### 3. Ручной деплой на сервере

Если нужно задеплоить вручную, подключитесь к серверу и выполните:

```bash
ssh root@your-server-ip
cd ~/Lec7.com
./scripts/deploy-simple.sh
```

Или используйте основной скрипт:
```bash
./scripts/deploy.sh
```

---

## Что делает деплой

1. **Обновляет код** - выполняет `git pull` из репозитория
2. **Останавливает контейнеры** - `docker-compose down`
3. **Пересобирает приложение** - `docker-compose build app`
4. **Запускает контейнеры** - `docker-compose up -d`
5. **Применяет миграции БД** - `npx prisma migrate deploy`
6. **Показывает статус** - логи и статус контейнеров

---

## Проверка статуса деплоя

### Через GitHub Actions
1. Откройте репозиторий на GitHub
2. Перейдите во вкладку **Actions**
3. Найдите последний запуск **Deploy to Timeweb**
4. Откройте его, чтобы увидеть логи

### На сервере
```bash
# Статус контейнеров
docker-compose ps

# Логи приложения
docker-compose logs -f app

# Логи базы данных
docker-compose logs -f postgres
```

---

## Troubleshooting

### Проблема: GitHub Actions не может подключиться к серверу
- Проверьте, что SSH ключ добавлен в секреты GitHub
- Убедитесь, что публичный ключ добавлен на сервер в `~/.ssh/authorized_keys`
- Проверьте, что порт 22 открыт в firewall

### Проблема: Деплой падает с ошибкой
- Проверьте логи в GitHub Actions
- Подключитесь к серверу и проверьте логи: `docker-compose logs app`
- Убедитесь, что `.env` файл настроен правильно

### Проблема: Миграции не применяются
- Проверьте подключение к БД: `docker-compose exec app npx prisma db pull`
- Убедитесь, что PostgreSQL запущен: `docker-compose ps postgres`

---

## Быстрая справка

```bash
# Автоматический деплой (просто push)
git push

# Локальный деплой (Linux/Mac)
./scripts/deploy-local.sh

# Локальный деплой (Windows)
.\scripts\deploy-local.ps1

# Ручной деплой на сервере
ssh root@server
cd ~/Lec7.com
./scripts/deploy-simple.sh
```

---

## Готово! 🎉

Теперь у вас есть три способа деплоя:
1. ✅ **Автоматический** - через GitHub Actions при каждом push
2. ✅ **Локальный** - через терминал с помощью скриптов
3. ✅ **Ручной** - напрямую на сервере

Выберите удобный для вас способ!
