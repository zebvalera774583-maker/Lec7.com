# 🚀 Автоматический деплой на Timeweb

После настройки ты просто пушишь в GitHub → сервер автоматически обновляется.

## Настройка (один раз)

### 1. Создай SSH ключ для GitHub Actions

На своём компьютере выполни:

```bash
ssh-keygen -t ed25519 -C "github-actions" -f ~/.ssh/timeweb_deploy -N ""
```

Это создаст два файла:
- `~/.ssh/timeweb_deploy` (приватный ключ)
- `~/.ssh/timeweb_deploy.pub` (публичный ключ)

### 2. Добавь публичный ключ на сервер Timeweb

**Скопируй публичный ключ:**
```bash
cat ~/.ssh/timeweb_deploy.pub
```

**На сервере Timeweb (в консоли):**
```bash
mkdir -p ~/.ssh
echo "ТВОЙ_ПУБЛИЧНЫЙ_КЛЮЧ" >> ~/.ssh/authorized_keys
chmod 600 ~/.ssh/authorized_keys
chmod 700 ~/.ssh
```

### 3. Добавь секреты в GitHub

1. Открой репозиторий на GitHub: `https://github.com/zebvalera774583-maker/Lec7.com`
2. Перейди в **Settings** → **Secrets and variables** → **Actions**
3. Добавь три секрета:

   - **`TIMEWEB_HOST`**: `194.87.104.179`
   - **`TIMEWEB_USER`**: `root`
   - **`TIMEWEB_SSH_KEY`**: содержимое файла `~/.ssh/timeweb_deploy` (приватный ключ)

### 4. Скопируй скрипт деплоя на сервер

**На сервере Timeweb:**
```bash
cd ~/Lec7.com
git pull
chmod +x scripts/deploy.sh
```

## Готово! 🎉

Теперь при каждом `git push` в ветку `main`:
1. GitHub Actions автоматически подключится к серверу
2. Обновит код (`git pull`)
3. Пересоберёт и перезапустит контейнеры
4. Применит миграции БД

## Как использовать

Просто работай как обычно:

```bash
# Внесли изменения
git add .
git commit -m "Добавил новую фичу"
git push
```

Всё! Через 5-10 минут сайт обновится автоматически.

## Проверка статуса деплоя

1. Открой репозиторий на GitHub
2. Перейди во вкладку **Actions**
3. Там будут видны все деплои и их статус

## Ручной деплой (если нужно)

Если нужно задеплоить вручную на сервере:

```bash
cd ~/Lec7.com
./scripts/deploy.sh
```

Или через GitHub Actions:
1. Открой **Actions** в репозитории
2. Выбери workflow **Deploy to Timeweb**
3. Нажми **Run workflow** → **Run workflow**
