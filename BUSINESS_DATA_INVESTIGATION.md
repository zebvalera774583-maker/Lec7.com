# 🔍 Расследование: Потерянные данные Business (6 записей)

## 📊 Результаты анализа репозитория

### ✅ Что найдено

#### 1. **CSV файл с данными бизнеса**
- **Файл:** `lec7-business-inventory.csv`
- **Содержимое:** 1 запись о бизнесе "прима" (id: `cmk9jmlso00022la7mk0cwdbu`)
- **Дата создания:** 2026-01-11 09:40:19
- **Git история:** Файл был добавлен в коммите `1ad60ae`
- **Вывод:** Это может быть экспорт данных, но содержит только 1 бизнес, а не 6

#### 2. **Статический контент на главной странице**
- **Файл:** `app/page.tsx` (строки 3-34)
- **Содержимое:** Хардкод массив из **6 карточек** бизнесов:
  1. Мебельный магазин «Forma» (Москва)
  2. Дизайн агентство «Line Studio» (Санкт-Петербург)
  3. Магазин одежды «Nord Wear» (Казань)
  4. Юридическая компания «Право» (Москва)
  5. IT студия «CodePoint» (Новосибирск)
  6. Студия интерьера «Space» (Екатеринбург)
- **Вывод:** Это **статический контент**, не реальные данные из БД. Возможно, админ видел эти карточки на главной странице и думал, что это данные из БД.

#### 3. **Критическая находка: скрипт deploy-fresh.sh**
- **Файл:** `scripts/deploy-fresh.sh`
- **Строка 9:** `docker system prune -af --volumes 2>/dev/null || true`
- **Что делает:** **УДАЛЯЕТ ВСЕ Docker volumes**, включая `lec7com_postgres_data`
- **Git история:** 
  - Коммит `ed79ee0`: "Fix: remove docker system prune from auto deploy"
  - Это означает, что проблема была обнаружена и исправлена
- **Вывод:** Если этот скрипт был запущен, все данные в volumes были удалены.

---

## ❌ Что НЕ найдено

1. **Seed-скрипты:** Нет файлов `seed.ts`, `seed.js`, `seed.sql`
2. **Дампы БД:** Нет SQL-дампов в репозитории (кроме миграций)
3. **SQL с INSERT:** Нет SQL-файлов с INSERT INTO Business
4. **Тестовые данные в коде:** Нет хардкод создания бизнесов в API (кроме статического контента на главной)

---

## 🔍 Гипотезы о потере данных

### Гипотеза 1: Запуск deploy-fresh.sh (ВЫСОКАЯ ВЕРОЯТНОСТЬ)
**Признаки:**
- Скрипт содержит `docker system prune -af --volumes`
- В git есть коммит, который убирает эту команду из auto-deploy
- Это объясняет полную потерю данных

**Проверка:**
```bash
# На сервере проверить историю команд
history | grep "deploy-fresh\|prune\|volume"

# Проверить, когда был последний запуск
ls -la ~/Lec7.com/scripts/deploy-fresh.sh
stat ~/Lec7.com/scripts/deploy-fresh.sh
```

### Гипотеза 2: Другой docker-compose project name (СРЕДНЯЯ ВЕРОЯТНОСТЬ)
**Признаки:**
- Текущий volume: `lec7com_postgres_data`
- Если проект запускался с другим именем, volume будет другим

**Проверка:**
```bash
# На сервере найти ВСЕ postgres volumes
docker volume ls | grep postgres

# Проверить содержимое каждого volume
docker run --rm -v <volume_name>:/data alpine ls -la /data
```

### Гипотеза 3: Другой путь проекта (СРЕДНЯЯ ВЕРОЯТНОСТЬ)
**Признаки:**
- Проект мог быть в другой папке (не `~/Lec7.com`)
- Volume мог быть создан из другой директории

**Проверка:**
```bash
# Найти все директории с docker-compose.yml
find ~ -name "docker-compose.yml" -type f 2>/dev/null

# Проверить, есть ли там volumes
for dir in $(find ~ -name "docker-compose.yml" -type f -exec dirname {} \;); do
  echo "=== $dir ==="
  cd "$dir" && docker-compose ps 2>/dev/null || true
done
```

### Гипотеза 4: Другой сервер/VPS (НИЗКАЯ ВЕРОЯТНОСТЬ)
**Признаки:**
- Если данные были на другом сервере
- Нужно проверить все доступные серверы

**Проверка:**
- Проверить все VPS в Timeweb панели
- Проверить SSH доступ к другим серверам

### Гипотеза 5: Данные были только в памяти/не сохранились (НИЗКАЯ ВЕРОЯТНОСТЬ)
**Признаки:**
- Бизнесы создавались через API, но миграции не применялись
- Или база была пересоздана без бэкапа

**Проверка:**
```bash
# Проверить историю миграций в БД
docker-compose exec postgres psql -U lec7 -d lec7 -c "SELECT * FROM _prisma_migrations ORDER BY finished_at DESC LIMIT 10;"
```

---

## 🛠️ Конкретные команды для поиска

### 1. Проверить все Docker volumes на сервере
```bash
# Список всех volumes
docker volume ls

# Детальная информация о каждом postgres volume
docker volume inspect <volume_name>

# Попытка подключиться к данным в volume
docker run --rm -it -v <volume_name>:/var/lib/postgresql/data postgres:15 psql -U lec7 -d lec7 -c "SELECT COUNT(*) FROM \"Business\";"
```

### 2. Проверить историю команд на сервере
```bash
# История bash
history | grep -E "deploy|prune|volume|docker.*down|compose.*down"

# История в других shell
cat ~/.bash_history | grep -E "deploy|prune|volume"
cat ~/.zsh_history | grep -E "deploy|prune|volume" 2>/dev/null || true
```

### 3. Проверить логи Docker
```bash
# Логи контейнеров postgres
docker logs $(docker ps -a | grep postgres | awk '{print $1}') | tail -100

# Системные логи Docker
journalctl -u docker.service | grep -i "volume\|prune" | tail -50
```

### 4. Проверить бэкапы (если были)
```bash
# Искать SQL дампы
find ~ -name "*.sql" -o -name "*.sql.gz" -o -name "*backup*" 2>/dev/null

# Проверить скрипт backup.sh
ls -la ~/Lec7.com/scripts/backup.sh
cat ~/Lec7.com/scripts/backup.sh

# Проверить директорию backups
ls -la ~/Lec7.com/backups/ 2>/dev/null || echo "Директория backups не найдена"
```

### 5. Проверить git историю на сервере
```bash
cd ~/Lec7.com
git log --all --oneline --since="2025-01-01" | head -50
git log --all --grep="deploy\|prune\|volume" -i
```

### 6. Проверить другие проекты с docker-compose
```bash
# Найти все docker-compose файлы
find ~ -name "docker-compose.yml" -type f 2>/dev/null

# Для каждого проверить volumes
for file in $(find ~ -name "docker-compose.yml" -type f 2>/dev/null); do
  echo "=== $file ==="
  grep -A 5 "postgres_data:" "$file" || echo "Нет postgres_data"
done
```

### 7. Проверить CSV файл детально
```bash
cd ~/Lec7.com
cat lec7-business-inventory.csv
git log --all -p -- "lec7-business-inventory.csv"
git show 1ad60ae:lec7-business-inventory.csv
```

---

## 📋 Выводы

### Что точно известно:
1. ✅ В репозитории **нет seed-скриптов** для создания бизнесов
2. ✅ В репозитории **нет SQL-дампов** с данными бизнесов
3. ✅ На главной странице есть **статический массив из 6 карточек** (не из БД)
4. ✅ Есть **CSV файл** с 1 бизнесом (не 6)
5. ✅ Скрипт `deploy-fresh.sh` **удаляет все volumes** (`docker system prune -af --volumes`)

### Наиболее вероятный сценарий:
**Данные были удалены при запуске `deploy-fresh.sh`**, который содержит команду `docker system prune -af --volumes`. Эта команда удаляет ВСЕ неиспользуемые volumes, включая данные PostgreSQL.

### Рекомендации:
1. **Немедленно проверить** историю команд на сервере (см. команды выше)
2. **Проверить все Docker volumes** на сервере - возможно, данные в другом volume
3. **Проверить бэкапы** - возможно, скрипт `backup.sh` создавал дампы
4. **Если данных нет** - восстановить из CSV файла (1 бизнес) или создать заново

---

## 🚨 Критическое замечание

**Скрипт `deploy-fresh.sh` опасен для production!**

Строка 9: `docker system prune -af --volumes` удаляет ВСЕ volumes, включая данные БД.

**Рекомендация:** Удалить или закомментировать эту строку в production окружении.

---

## 📝 Следующие шаги

1. Выполнить команды из раздела "Конкретные команды для поиска" на сервере
2. Если данные не найдены - восстановить из CSV (1 бизнес) или создать заново
3. Настроить автоматические бэкапы через `scripts/backup.sh`
4. Убрать `docker system prune --volumes` из всех deploy-скриптов
