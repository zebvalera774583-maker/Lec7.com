# 🚀 Деплой Lec7 на Timeweb - Пошаговая инструкция

## Порядок действий:

1. ✅ **GitHub** - загрузить код в репозиторий
2. ✅ **Timeweb** - настроить VPS и задеплоить

---

## Шаг 1: GitHub (Сначала это!) 📦

### 1.1. Создайте репозиторий на GitHub

1. Зайдите на [github.com](https://github.com)
2. Нажмите "New repository"
3. Название: `lec7` (или любое другое)
4. Выберите **Private** (или Public, если хотите)
5. **НЕ** добавляйте README, .gitignore, лицензию (у нас уже есть)
6. Нажмите "Create repository"

### 1.2. Закоммитьте и загрузите код

Выполните в терминале (в папке проекта):

```bash
# Инициализируем Git (если еще не сделано)
git init

# Добавляем все файлы
git add .

# Создаём первый коммит
git commit -m "Initial commit: Lec7 v1 ready for deploy"

# Добавляем удалённый репозиторий (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/lec7.git

# Загружаем код
git branch -M main
git push -u origin main
```

**Важно:** Замените `YOUR_USERNAME` на ваш реальный GitHub username!

### 1.3. Проверка

Зайдите на GitHub и убедитесь, что все файлы загружены.

---

## Шаг 2: Timeweb VPS 🖥️

### 2.1. Создайте VPS в Timeweb

1. Зайдите в [панель Timeweb](https://timeweb.com)
2. Перейдите в раздел **VPS**
3. Нажмите **"Создать VPS"**
4. Выберите конфигурацию:
   - **Минимум**: 2 GB RAM, 2 CPU, 20 GB SSD
   - **Рекомендуется**: 4 GB RAM, 4 CPU, 40 GB SSD
5. Выберите ОС: **Ubuntu 22.04** (или новее)
6. Выберите регион (ближайший к вам)
7. Нажмите **"Создать"**

### 2.2. Подключитесь к серверу

После создания VPS:

1. В панели Timeweb найдите IP-адрес вашего VPS
2. Найдите пароль (обычно отправляется на email или в панели)

Подключитесь через SSH:

**Windows (PowerShell или Git Bash):**
```bash
ssh root@YOUR_SERVER_IP
```

**Или используйте PuTTY:**
- Host: `YOUR_SERVER_IP`
- Port: `22`
- Username: `root`
- Password: (из панели Timeweb)

### 2.3. Установите Docker и Docker Compose

На сервере выполните:

```bash
# Обновляем систему
apt update && apt upgrade -y

# Устанавливаем Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh

# Устанавливаем Docker Compose
curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
chmod +x /usr/local/bin/docker-compose

# Проверяем установку
docker --version
docker-compose --version
```

### 2.4. Клонируйте проект с GitHub

```bash
# Переходим в домашнюю директорию
cd ~

# Клонируем проект (замените YOUR_USERNAME на ваш GitHub username)
git clone https://github.com/YOUR_USERNAME/lec7.git

# Переходим в папку проекта
cd lec7
```

---

## Шаг 3: Настройка проекта ⚙️

### 3.1. Создайте .env файл

```bash
# Создаём файл
nano .env
```

Вставьте следующее (замените значения на свои):

```env
# База данных (будет в Docker, оставляем как есть)
DATABASE_URL=postgresql://lec7:CHANGE_THIS_PASSWORD@postgres:5432/lec7?schema=public

# URL приложения (замените на ваш домен или IP)
NEXT_PUBLIC_APP_URL=http://YOUR_SERVER_IP:3000

# JWT секрет (сгенерируйте случайную строку минимум 32 символа)
JWT_SECRET=your-super-secret-jwt-key-minimum-32-characters-long

# OpenAI API ключ
OPENAI_API_KEY=sk-your-openai-api-key-here

# Timeweb S3 настройки (создайте бакет в панели Timeweb)
S3_ENDPOINT=https://s3.timeweb.com
S3_ACCESS_KEY_ID=your-timeweb-s3-access-key
S3_SECRET_ACCESS_KEY=your-timeweb-s3-secret-key
S3_BUCKET_NAME=lec7-storage
S3_REGION=ru-1
S3_PUBLIC_URL=https://lec7-storage.s3.timeweb.com
```

**Как сохранить в nano:**
- Нажмите `Ctrl + O` (сохранить)
- Нажмите `Enter` (подтвердить имя файла)
- Нажмите `Ctrl + X` (выйти)

### 3.2. Настройте Timeweb S3

1. В панели Timeweb перейдите в **"Облачное хранилище"** или **"S3"**
2. Создайте новый бакет:
   - Название: `lec7-storage` (или любое другое)
   - Регион: выберите ближайший
3. Получите ключи доступа (Access Key ID и Secret Access Key)
4. Включите публичный доступ для чтения (для портфолио)
5. Скопируйте ключи в `.env` файл

### 3.3. Обновите docker-compose.yml для production

Откройте файл:
```bash
nano docker-compose.yml
```

Измените строку с `NEXT_PUBLIC_APP_URL` на ваш реальный URL:
```yaml
NEXT_PUBLIC_APP_URL: http://YOUR_SERVER_IP:3000
# или если есть домен:
NEXT_PUBLIC_APP_URL: https://your-domain.com
```

Сохраните (`Ctrl + O`, `Enter`, `Ctrl + X`)

---

## Шаг 4: Деплой 🚀

### 4.1. Запустите проект

```bash
# Собираем и запускаем контейнеры
docker-compose up -d --build

# Ждём 10 секунд, пока база данных запустится
sleep 10

# Применяем миграции базы данных
docker-compose exec app npx prisma migrate deploy
```

### 4.2. Проверьте работу

```bash
# Проверяем статус контейнеров
docker-compose ps

# Смотрим логи
docker-compose logs -f app
```

Откройте в браузере: `http://YOUR_SERVER_IP:3000`

---

## Шаг 5: Настройка домена (опционально) 🌐

### 5.1. Подключите домен в Timeweb

1. В панели Timeweb перейдите в **"Домены"**
2. Добавьте ваш домен
3. Настройте DNS записи:
   - Тип: `A`
   - Имя: `@` (или `www`)
   - Значение: IP вашего VPS

### 5.2. Установите Nginx

```bash
# Устанавливаем Nginx
apt install nginx -y

# Создаём конфигурацию
nano /etc/nginx/sites-available/lec7
```

Вставьте (замените `your-domain.com` на ваш домен):

```nginx
server {
    listen 80;
    server_name your-domain.com www.your-domain.com;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}
```

Сохраните и активируйте:

```bash
# Создаём симлинк
ln -s /etc/nginx/sites-available/lec7 /etc/nginx/sites-enabled/

# Проверяем конфигурацию
nginx -t

# Перезагружаем Nginx
systemctl reload nginx
```

### 5.3. Установите SSL сертификат (Let's Encrypt)

```bash
# Устанавливаем Certbot
apt install certbot python3-certbot-nginx -y

# Получаем сертификат
certbot --nginx -d your-domain.com -d www.your-domain.com

# Автоматическое обновление (уже настроено)
```

### 5.4. Обновите .env

Измените `NEXT_PUBLIC_APP_URL` на ваш домен:
```env
NEXT_PUBLIC_APP_URL=https://your-domain.com
```

Перезапустите:
```bash
docker-compose restart app
```

---

## Шаг 6: Обновление проекта 🔄

Когда нужно обновить код:

```bash
# Переходим в папку проекта
cd ~/lec7

# Получаем последние изменения с GitHub
git pull

# Пересобираем и перезапускаем
docker-compose up -d --build

# Применяем новые миграции (если есть)
docker-compose exec app npx prisma migrate deploy
```

---

## Полезные команды 📋

```bash
# Просмотр логов
docker-compose logs -f app

# Остановка
docker-compose down

# Запуск
docker-compose up -d

# Перезапуск
docker-compose restart

# Вход в контейнер приложения
docker-compose exec app sh

# Резервное копирование БД
docker-compose exec postgres pg_dump -U lec7 lec7 > backup.sql
```

---

## Troubleshooting 🔧

### Проблема: Не могу подключиться по SSH
- Проверьте, что в Timeweb открыт порт 22
- Убедитесь, что используете правильный пароль

### Проблема: Приложение не запускается
```bash
# Проверьте логи
docker-compose logs app

# Проверьте переменные окружения
docker-compose exec app env | grep DATABASE_URL
```

### Проблема: База данных недоступна
```bash
# Проверьте статус PostgreSQL
docker-compose ps postgres

# Перезапустите
docker-compose restart postgres
```

### Проблема: S3 не работает
- Проверьте ключи доступа в `.env`
- Убедитесь, что бакет существует
- Проверьте права доступа к бакету

---

## Готово! ✅

Ваше приложение должно быть доступно по адресу:
- `http://YOUR_SERVER_IP:3000` (без домена)
- `https://your-domain.com` (с доменом и SSL)
