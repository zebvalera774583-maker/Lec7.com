# 🔧 DevOps: Исправление контейнера app

## Проблема
После `docker-compose up -d --build` сервис `app` не считается running.

## Решение

### Вариант 1: Автоматическое исправление (рекомендуется)

На сервере выполните:

```bash
cd ~/Lec7.com
git pull  # Получить обновлённый docker-entrypoint.sh
chmod +x scripts/devops-fix-app.sh
./scripts/devops-fix-app.sh
```

Этот скрипт:
1. Остановит контейнеры
2. Пересоберёт app
3. Запустит PostgreSQL
4. Запустит app
5. Проверит статус

### Вариант 2: Ручное исправление

```bash
cd ~/Lec7.com

# 1. Остановить контейнеры
docker-compose down

# 2. Обновить docker-entrypoint.sh (если нужно)
git pull

# 3. Пересобрать app
docker-compose build --no-cache app

# 4. Запустить PostgreSQL
docker-compose up -d postgres
sleep 20

# 5. Запустить app
docker-compose up -d app
sleep 15

# 6. Проверить статус
docker-compose ps
docker-compose logs app --tail=50
```

### Вариант 3: Диагностика перед исправлением

```bash
cd ~/Lec7.com
chmod +x scripts/diagnose-app-container.sh
./scripts/diagnose-app-container.sh
```

## Что было исправлено

### 1. Улучшен `docker-entrypoint.sh`

**Проблема:** Скрипт падал, если миграции уже применены или БД недоступна.

**Исправление:**
- Добавлена обработка ошибок для `prisma migrate deploy`
- Добавлен fallback на `prisma db push`
- Добавлена проверка наличия `server.js`
- Улучшено логирование ошибок

### 2. Созданы диагностические скрипты

- `scripts/diagnose-app-container.sh` - диагностика проблем
- `scripts/devops-fix-app.sh` - автоматическое исправление
- `scripts/fix-app-container.sh` - альтернативный скрипт исправления

## Проверка после исправления

```bash
# 1. Статус контейнеров
docker-compose ps

# Должно быть:
# lec7-postgres    Up (healthy)
# lec7-app         Up (running)

# 2. Проверка доступности контейнера
docker-compose exec app sh
# Если команда выполнилась - контейнер работает

# 3. HTTP проверка
curl http://localhost:3000/owner
curl http://localhost:3000/owner/businesses
```

## Типичные проблемы и решения

### Проблема: "server.js not found"

**Причина:** Next.js не собрался в standalone режиме.

**Решение:**
```bash
docker-compose build --no-cache app
```

### Проблема: "Migrations failed"

**Причина:** База данных недоступна или миграции конфликтуют.

**Решение:**
```bash
# Проверить PostgreSQL
docker-compose ps postgres
docker-compose logs postgres

# Если PostgreSQL не запущен:
docker-compose up -d postgres
sleep 20
docker-compose up -d app
```

### Проблема: Контейнер постоянно перезапускается

**Причина:** Ошибка в коде или отсутствие переменных окружения.

**Решение:**
```bash
# Посмотреть логи
docker-compose logs app --tail=100

# Проверить .env файл
cat .env

# Пересобрать с нуля
docker-compose down
docker-compose build --no-cache app
docker-compose up -d
```

## После успешного исправления

✅ Контейнер `lec7-app` должен быть в статусе `Up`
✅ Команда `docker-compose exec app sh` должна работать
✅ HTTP запросы к `/owner` и `/owner/businesses` должны отвечать
