# Ревизия проекта Lec7.com

**Дата:** 2025-01-XX  
**Цель:** Карта используемых и неиспользуемых компонентов проекта

---

## ✅ ВЫПОЛНЕННАЯ ЧИСТКА (2025-01-XX)

### Удалённые файлы:
1. `app/(public)/b/[slug]/page.tsx` — дубликат маршрута `/biz/[slug]`
2. `app/(public)/b/[slug]/layout.tsx` — layout для удалённого маршрута
3. `components/BusinessCard.tsx` — не использовался (используется `BusinessCardLink`)
4. `components/ui/` — вся папка (6 файлов):
   - `AIBadge.tsx`
   - `AIHero.tsx`
   - `Button.tsx`
   - `Card.tsx`
   - `index.ts`
   - `Metric.tsx`

### Удалённые зависимости:
1. `@aws-sdk/client-s3` — не использовался
2. `@aws-sdk/s3-request-presigner` — не использовался
3. `date-fns` — не использовался
4. `zod` — не использовался

### Проверки:
- ✅ `npm run build` — успешно (exit code: 0)
- ⚠️ ESLint warnings (не критично):
  - `app/visitor/VisitorClient.tsx` — missing dependencies в useEffect
  - `components/PortfolioGallery.tsx` — использование `<img>` вместо `<Image />`

### Что НЕ удалено и почему:

1. **`app/api/portfolio/route.ts`** — НЕ удалён
   - **Причина:** Может быть использован в будущем для `/biz/[slug]`
   - **Статус:** GET работает, POST отключен (S3 закомментирован)
   - **Использование:** Только в удалённом маршруте `/b/[slug]`, но оставлен для будущего функционала

2. **Prisma модели (`Event`, `Document`, `BusinessUser`)** — НЕ тронуты
   - **Причина:** По требованию — не менять Prisma модели и миграции
   - **Статус:** Определены в schema, но не используются в коде

3. **Компоненты `PortfolioGallery` и `AIChat`** — НЕ удалены
   - **Причина:** Могут быть использованы в будущем для `/biz/[slug]`
   - **Статус:** Использовались только в удалённом маршруте `/b/[slug]`

---

---

## 📍 МАРШРУТЫ (app/**/page.tsx)

### ✅ Используется и критично

1. **app/page.tsx** — Главная страница
   - Ссылки на `/visitor` и `/owner/welcome`
   - Используется: `HoverLink`

2. **app/visitor/page.tsx** — Список бизнесов для посетителей
   - Использует: `VisitorClient`, API `/api/businesses`
   - Ссылки на `/biz/[slug]`

3. **app/login/page.tsx** — Авторизация/регистрация
   - Использует: API `/api/auth/login`, `/api/auth/register`
   - Редирект на `/office`

4. **app/office/page.tsx** — Личный кабинет владельца
   - Использует: Prisma `Business`, `Request`, `Invoice`
   - Ссылки на `/office/businesses/[id]`

5. **app/office/businesses/[id]/page.tsx** — Детали бизнеса в офисе
   - Использует: Prisma `Business`, `Request`, `Invoice`, `Portfolio`

6. **app/admin/page.tsx** — Админ-панель (статистика)
   - Использует: Prisma `Business`, `User`, `Request`, `Invoice`

7. **app/admin/owner-agent/page.tsx** — Owner Agent (AI-помощник)
   - Использует: API `/api/owner-agent`

8. **app/owner/welcome/page.tsx** — Приветственная страница для владельцев
   - Ссылка на `/owner/admin`

9. **app/owner/admin/page.tsx** — Админка бизнеса (wizard создания)
   - Использует: API `/api/businesses` (GET, POST)
   - Ссылки на `/biz/[slug]`

10. **app/biz/[slug]/page.tsx** — Публичная страница бизнеса
    - Использует: Prisma `Business`
    - Ссылки на `/visitor`

### ⚠️ Под вопросом

11. **app/(public)/b/[slug]/page.tsx** — Дубликат публичной страницы
    - **Проблема:** В коде используются ссылки на `/biz/[slug]`, а не `/b/[slug]`
    - **Статус:** Похоже на старый маршрут, который не используется
    - **Рекомендация:** Проверить, есть ли внешние ссылки на `/b/`, если нет — удалить

---

## 🔌 API РОУТЫ (app/api/**/route.ts)

### ✅ Используется и критично

1. **app/api/businesses/route.ts** — CRUD бизнесов
   - GET: используется в `/visitor`, `/owner/admin`
   - POST: используется в `/owner/admin`
   - Использует: Prisma `Business`, `User`

2. **app/api/auth/login/route.ts** — Авторизация
   - Используется в `/login`
   - Использует: `lib/auth.ts` (bcryptjs, jsonwebtoken)

3. **app/api/auth/register/route.ts** — Регистрация
   - Используется в `/login`
   - Использует: `lib/auth.ts`

4. **app/api/auth/logout/route.ts** — Выход
   - Использует: cookies

5. **app/api/owner-agent/route.ts** — Owner Agent API
   - Используется в `/admin/owner-agent`
   - Использует: `lib/openai.ts` (OpenAI API)

6. **app/api/requests/route.ts** — Создание заявок
   - POST: используется в `AIChat` компоненте
   - Использует: Prisma `Request`

7. **app/api/invoices/route.ts** — Создание и получение счетов
   - GET, POST: используется в `/office/businesses/[id]`
   - Использует: Prisma `Invoice`

8. **app/api/ai/chat/route.ts** — AI-чат для бизнесов
   - Используется в `AIChat` компоненте
   - Использует: `lib/openai.ts`, Prisma `Business`

### ⚠️ Под вопросом

9. **app/api/portfolio/route.ts** — Портфолио
   - GET: используется в `app/(public)/b/[slug]/page.tsx` (но этот маршрут не используется)
   - POST: отключен (закомментирован S3)
   - **Рекомендация:** Если `/b/[slug]` удаляется, то GET тоже можно удалить или перенести на `/biz/[slug]`

---

## 🗄️ МОДЕЛИ PRISMA

### ✅ Используется и критично

1. **User** — Пользователи
   - Используется в: `lib/auth.ts`, `/api/auth/*`, `/api/businesses`, `/admin`

2. **Business** — Бизнесы
   - Используется везде: `/visitor`, `/biz/[slug]`, `/office`, `/api/businesses`, `/api/ai/chat`

3. **Portfolio** — Портфолио бизнесов
   - Используется в: `/api/portfolio`, `app/(public)/b/[slug]/page.tsx` (неиспользуемый маршрут)

4. **Request** — Заявки клиентов
   - Используется в: `/api/requests`, `/office`, `/admin`

5. **Invoice** — Счета
   - Используется в: `/api/invoices`, `/office`, `/admin`

### ❌ Похоже на мусор / не используется

6. **Event** — События
   - **Статус:** Определена в schema, но нигде не используется
   - **Рекомендация:** Удалить из schema или реализовать функционал

7. **Document** — Документы
   - **Статус:** Определена в schema, но нигде не используется
   - **Рекомендация:** Удалить из schema или реализовать функционал

8. **BusinessUser** — Связь бизнеса и пользователя (многие-ко-многим)
   - **Статус:** Определена в schema, но нигде не используется
   - **Рекомендация:** Удалить из schema или реализовать функционал (если нужна поддержка нескольких владельцев)

---

## 📦 ЗАВИСИМОСТИ (package.json)

### ✅ Используется и критично

1. **@prisma/client** — Prisma ORM
2. **prisma** — Prisma CLI
3. **next** — Next.js фреймворк
4. **react** — React
5. **react-dom** — React DOM
6. **bcryptjs** — Хеширование паролей (используется в `lib/auth.ts`)
7. **jsonwebtoken** — JWT токены (используется в `lib/auth.ts`)
8. **openai** — OpenAI API (используется в `lib/openai.ts`)
9. **react-markdown** — Рендеринг markdown (используется в `OwnerAgentClient.tsx`)

### ❌ Похоже на мусор / не используется

10. **@aws-sdk/client-s3** — AWS S3 клиент
    - **Статус:** Импортирован в `app/api/portfolio/route.ts`, но закомментирован
    - **Рекомендация:** Удалить, если S3 не планируется использовать

11. **@aws-sdk/s3-request-presigner** — AWS S3 presigner
    - **Статус:** Не используется
    - **Рекомендация:** Удалить вместе с client-s3

12. **date-fns** — Работа с датами
    - **Статус:** Не используется нигде в коде
    - **Рекомендация:** Удалить

13. **zod** — Валидация схем
    - **Статус:** Не используется нигде в коде
    - **Рекомендация:** Удалить

### ✅ DevDependencies (все используются)

- `@types/*` — TypeScript типы
- `eslint`, `eslint-config-next` — Линтинг
- `typescript` — TypeScript

---

## 🧩 КОМПОНЕНТЫ (components/)

### ✅ Используется и критично

1. **components/HoverLink.tsx** — Ссылка с hover-эффектом
   - Используется в: `app/page.tsx`, `app/owner/welcome/page.tsx`

2. **components/BusinessCardLink.tsx** — Карточка бизнеса со ссылкой
   - Используется в: `app/visitor/VisitorClient.tsx`

3. **components/PortfolioGallery.tsx** — Галерея портфолио
   - Используется в: `app/(public)/b/[slug]/page.tsx` (но маршрут не используется)

4. **components/AIChat.tsx** — AI-чат для бизнесов
   - Используется в: `app/(public)/b/[slug]/page.tsx` (но маршрут не используется)

### ⚠️ Под вопросом

5. **components/BusinessCard.tsx** — Карточка бизнеса (без ссылки)
   - **Статус:** Определён, но не используется (используется `BusinessCardLink`)
   - **Рекомендация:** Удалить или использовать, если нужна карточка без ссылки

### ❌ Похоже на мусор / не используется

6. **components/ui/** — UI компоненты
   - **Содержимое:** `AIBadge.tsx`, `AIHero.tsx`, `Button.tsx`, `Card.tsx`, `Metric.tsx`, `index.ts`
   - **Статус:** Не используются нигде в коде
   - **Рекомендация:** Удалить или использовать в проекте

---

## 📊 СВОДНАЯ ТАБЛИЦА

| Категория | Критично | Под вопросом | Мусор |
|-----------|----------|--------------|-------|
| **Маршруты** | 10 | 1 | 0 |
| **API** | 8 | 1 | 0 |
| **Prisma модели** | 5 | 0 | 3 |
| **Зависимости** | 9 | 0 | 4 |
| **Компоненты** | 4 | 1 | 1 (папка ui/) |

---

## 🎯 РЕКОМЕНДАЦИИ

### Немедленные действия (безопасно удалить)

1. **Удалить неиспользуемые зависимости:**
   ```bash
   npm uninstall @aws-sdk/client-s3 @aws-sdk/s3-request-presigner date-fns zod
   ```

2. **Удалить неиспользуемые компоненты:**
   - `components/BusinessCard.tsx` (если не нужен)
   - `components/ui/` (вся папка, если не планируется использовать)

3. **Удалить неиспользуемый маршрут:**
   - `app/(public)/b/[slug]/page.tsx` и `app/(public)/b/[slug]/layout.tsx`
   - Проверить, что нет внешних ссылок на `/b/[slug]`

### Долгосрочные решения

4. **Prisma модели:**
   - Решить судьбу `Event`, `Document`, `BusinessUser`
   - Либо удалить из schema, либо реализовать функционал

5. **API Portfolio:**
   - Если удаляется `/b/[slug]`, то либо удалить `/api/portfolio`, либо перенести на `/biz/[slug]`

6. **S3 интеграция:**
   - Если не планируется использовать S3, удалить все упоминания
   - Если планируется — раскомментировать и доработать

---

## ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ

1. **Дублирование маршрутов:** `/biz/[slug]` и `/b/[slug]` делают одно и то же, но используется только первый.

2. **Неиспользуемые Prisma модели:** 3 модели определены, но не используются. Это может создавать путаницу и лишние миграции.

3. **UI компоненты:** Папка `components/ui/` не используется, но может быть полезна для будущего развития проекта.

4. **S3 отключен:** Загрузка портфолио временно отключена. Нужно решить, будет ли это использоваться.

---

---

## 📋 РЕЗУЛЬТАТЫ ЧИСТКИ

### Удалённые файлы (9):
1. `app/(public)/b/[slug]/page.tsx`
2. `app/(public)/b/[slug]/layout.tsx`
3. `components/BusinessCard.tsx`
4. `components/ui/AIBadge.tsx`
5. `components/ui/AIHero.tsx`
6. `components/ui/Button.tsx`
7. `components/ui/Card.tsx`
8. `components/ui/index.ts`
9. `components/ui/Metric.tsx`

### Удалённые зависимости (4):
1. `@aws-sdk/client-s3`
2. `@aws-sdk/s3-request-presigner`
3. `date-fns`
4. `zod`

### Проверка сборки:
```bash
npm run build
```
**Результат:** ✅ Успешно (exit code: 0)

**Предупреждения (не критично):**
- ESLint warnings в `app/visitor/VisitorClient.tsx` (missing dependencies)
- ESLint warnings в `components/PortfolioGallery.tsx` (использование `<img>`)

### Что НЕ удалено:

1. **`app/api/portfolio/route.ts`**
   - **Причина:** Может быть использован в будущем
   - **Использование:** Только в удалённом маршруте `/b/[slug]`

2. **Prisma модели (`Event`, `Document`, `BusinessUser`)**
   - **Причина:** По требованию — не менять Prisma модели
   - **Статус:** Определены в schema, но не используются

3. **Компоненты `PortfolioGallery` и `AIChat`**
   - **Причина:** Могут быть использованы в будущем для `/biz/[slug]`
   - **Статус:** Использовались только в удалённом маршруте

**Конец отчёта**
