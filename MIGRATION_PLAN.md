# 🚀 План миграции с Timeweb на Cloudflare + Hetzner

## ✅ Что уже готово
- ✅ Docker Compose настроен
- ✅ Next.js standalone output
- ✅ PostgreSQL в контейнере
- ✅ Базовая структура для R2 (в docker-compose.yml)

---

## 📋 Пошаговый план (детализированный)

### Этап 1: Cloudflare DNS (15-20 мин)

**Что делаем:**
1. Регистрация в Cloudflare (free plan)
2. Добавление домена `lec7.com`
3. Получение NS-серверов
4. Прописывание NS в Timeweb

**Важно:**
- Домен остается у Timeweb как регистратора
- Только DNS переходит в Cloudflare
- Старый сайт может работать еще 1-2 часа (пока NS распространятся)

---

### Этап 2: VPS выбор и настройка (20-30 мин)

**Рекомендации:**

#### Вариант A: Hetzner (рекомендую)
- **Конфигурация:** CPX21 (4 vCPU, 8GB RAM, 160GB SSD)
- **Цена:** ~€8/мес
- **Регион:** Финляндия (ближе к России, но за границей)
- **Почему:** Лучший баланс цена/качество для Docker

#### Вариант B: DigitalOcean
- **Конфигурация:** Basic Droplet 4GB/2vCPU
- **Цена:** ~$24/мес
- **Почему:** Очень стабильно, но дороже

**Что делаем на VPS:**
```bash
# 1. Установка Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh

# 2. Установка Docker Compose
sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose

# 3. Настройка firewall
sudo ufw allow 22/tcp
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw enable
```

---

### Этап 3: Cloudflare R2 (Storage) (15 мин)

**Что делаем:**
1. Создать R2 bucket в Cloudflare
2. Получить Access Key ID и Secret
3. Настроить публичный домен для файлов (опционально)
4. **ВАЖНО:** Подготовить скрипт миграции данных из Timeweb S3

**Настройка R2:**
- Bucket name: `lec7-storage`
- Public access: включить (для портфолио)
- Custom domain: `files.lec7.com` (опционально, через Cloudflare)

---

### Этап 4: Миграция кода (30-40 мин)

**Что нужно изменить:**

1. **Обновить `lib/s3.ts`** для поддержки R2:
   - R2 использует `forcePathStyle: false` (в отличие от Timeweb)
   - Endpoint другой формат
   - Public URL через Cloudflare CDN

2. **Обновить `docker-compose.yml`:**
   - Заменить S3 переменные на R2
   - Обновить `NEXT_PUBLIC_APP_URL` на новый домен

3. **Добавить Caddy для SSL:**
   - Автоматический SSL через Let's Encrypt
   - Reverse proxy на `localhost:3000`

---

### Этап 5: Миграция данных (критично!)

**База данных:**
```bash
# На старом сервере (Timeweb)
docker-compose exec postgres pg_dump -U lec7 lec7 > backup_$(date +%Y%m%d).sql

# На новом сервере (Hetzner)
# Восстановление
docker-compose exec -T postgres psql -U lec7 lec7 < backup_YYYYMMDD.sql
```

**S3 файлы:**
- Нужен скрипт для копирования из Timeweb S3 → Cloudflare R2
- Обновить все URL в базе данных (если они хранятся)

---

### Этап 6: Reverse Proxy (Caddy) (15 мин)

**Создать `Caddyfile`:**
```caddy
lec7.com {
    reverse_proxy localhost:3000
}

www.lec7.com {
    reverse_proxy localhost:3000
}
```

**Добавить в `docker-compose.yml`:**
```yaml
caddy:
  image: caddy:latest
  container_name: lec7-caddy
  ports:
    - "80:80"
    - "443:443"
  volumes:
    - ./Caddyfile:/etc/caddy/Caddyfile
    - caddy_data:/data
    - caddy_config:/config
  restart: unless-stopped
```

---

### Этап 7: Деплой и тестирование (20 мин)

1. Клонировать репозиторий на новый VPS
2. Создать `.env` с новыми переменными
3. Запустить `docker-compose up -d --build`
4. Применить миграции: `docker-compose exec app npx prisma migrate deploy`
5. Проверить работу сайта

---

### Этап 8: DNS в Cloudflare (10 мин)

**Настроить записи:**
- `A` запись `@` → IP нового VPS
- `A` запись `www` → IP нового VPS
- SSL/TLS: **Full (strict)**

**Проверка:**
- Подождать 5-15 минут
- Проверить `https://lec7.com`
- Проверить SSL сертификат

---

## ⚠️ Критические моменты

### 1. Миграция S3 данных
**Проблема:** Если у вас уже есть загруженные файлы в Timeweb S3, их нужно перенести.

**Решение:** Скрипт миграции (нужно написать):
```bash
# Пример логики
# 1. Список всех файлов из Timeweb S3
# 2. Загрузка каждого в R2
# 3. Обновление URL в базе данных
```

### 2. Downtime
**Минимизация:**
- Сначала поднять новый сервер
- Протестировать всё
- Только потом переключать DNS
- Старый сервер оставить работающим 24-48 часов

### 3. Переменные окружения
**Важно:** Не забыть про:
- `LEC7_OWNER_EMAIL`
- `LEC7_OWNER_PASSWORD`
- `LEC7_AI_GATEWAY_URL`
- `LEC7_GATEWAY_SECRET`
- `OPENAI_API_KEY`

---

## 📊 Оценка времени

| Этап | Время | Критичность |
|------|-------|-------------|
| Cloudflare DNS | 20 мин | Высокая |
| VPS настройка | 30 мин | Высокая |
| R2 настройка | 15 мин | Высокая |
| Код миграция | 40 мин | Высокая |
| Данные миграция | 30-60 мин | **Критично** |
| Caddy настройка | 15 мин | Высокая |
| Деплой | 20 мин | Высокая |
| DNS переключение | 10 мин | Высокая |
| **ИТОГО** | **2-3 часа** | |

---

## 🎯 Рекомендации

### Что делать СЕЙЧАС:
1. ✅ Зарегистрироваться в Cloudflare
2. ✅ Настроить DNS (это можно сделать параллельно)
3. ✅ Выбрать и заказать VPS (Hetzner рекомендую)

### Что делать ПОТОМ:
1. Подготовить скрипт миграции S3 данных
2. Обновить код для R2
3. Протестировать на новом сервере
4. Переключить DNS

### Что НЕ делать:
- ❌ Не удалять старый сервер сразу
- ❌ Не переключать DNS до полного тестирования
- ❌ Не забыть про бэкап базы данных

---

## 🔧 Полезные команды

### Проверка работы
```bash
# Логи приложения
docker-compose logs -f app

# Статус контейнеров
docker-compose ps

# Проверка базы данных
docker-compose exec postgres psql -U lec7 -d lec7 -c "SELECT COUNT(*) FROM businesses;"
```

### Бэкап
```bash
# База данных
docker-compose exec postgres pg_dump -U lec7 lec7 > backup.sql

# Volumes (если нужно)
docker run --rm -v lec7_postgres_data:/data -v $(pwd):/backup alpine tar czf /backup/postgres_backup.tar.gz /data
```

---

## ✅ Чеклист перед переключением DNS

- [ ] Новый сервер работает
- [ ] SSL сертификат получен
- [ ] База данных мигрирована
- [ ] S3 файлы мигрированы (если есть)
- [ ] Все API работают
- [ ] Авторизация работает
- [ ] Загрузка файлов работает
- [ ] Бэкап старой базы данных сделан
- [ ] Старый сервер оставлен работающим

---

## 🆘 Rollback план

Если что-то пошло не так:

1. **В Cloudflare:** Вернуть старые NS от Timeweb
2. **Или:** Изменить A-запись обратно на старый IP
3. Время отката: 5-15 минут

---

## 📝 Следующие шаги

1. **Сейчас:** Начать с Cloudflare DNS (это безопасно)
2. **Параллельно:** Заказать VPS на Hetzner
3. **Потом:** Подготовить код и миграцию данных
4. **В конце:** Переключить DNS

---

**Время на всё:** 2-3 часа спокойной работы
**Downtime:** Минимальный (только переключение DNS, 5-15 мин)
