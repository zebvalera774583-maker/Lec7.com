#!/bin/bash

# Скрипт для резервного копирования базы данных

set -e

BACKUP_DIR="./backups"
DATE=$(date +%Y%m%d_%H%M%S)
BACKUP_FILE="$BACKUP_DIR/lec7_backup_$DATE.sql"

# Создаём директорию для бэкапов
mkdir -p $BACKUP_DIR

echo "💾 Создаём резервную копию базы данных..."

# Создаём бэкап
docker-compose exec -T postgres pg_dump -U lec7 lec7 > $BACKUP_FILE

# Сжимаем бэкап
gzip $BACKUP_FILE

echo "✅ Резервная копия создана: ${BACKUP_FILE}.gz"

# Удаляем старые бэкапы (старше 7 дней)
find $BACKUP_DIR -name "*.gz" -mtime +7 -delete

echo "🧹 Старые бэкапы (старше 7 дней) удалены"
