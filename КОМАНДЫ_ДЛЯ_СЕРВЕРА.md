# Команды для выполнения на сервере

## Способ 1: Консоль в панели Timeweb

1. Зайдите в панель Timeweb
2. Откройте ваш сервер "Brave Ganymede"
3. Перейдите на вкладку **"Консоль"**
4. Скопируйте и вставьте команды ниже

## Способ 2: Обычный SSH

1. Откройте любой терминал (Git Bash, PuTTY, или встроенный терминал)
2. Выполните: `ssh root@194.87.104.179`
3. Введите пароль
4. Скопируйте и выполните команды ниже

---

## Команды для выполнения:

```bash
# 1. Обновляем систему
apt update && apt upgrade -y

# 2. Устанавливаем Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh

# 3. Устанавливаем Docker Compose
curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
chmod +x /usr/local/bin/docker-compose

# 4. Проверяем установку
docker --version
docker-compose --version

# 5. Клонируем проект
cd ~
git clone https://github.com/zebvalera774583-maker/Lec7.com.git
cd Lec7.com
```

---

## После выполнения команд:

1. **Создайте .env файл:**
   ```bash
   nano .env
   ```
   (Вставьте настройки - см. SETUP_SERVER.md)

2. **Запустите проект:**
   ```bash
   docker-compose up -d --build
   ```

3. **Примените миграции:**
   ```bash
   docker-compose exec app npx prisma migrate deploy
   ```

---

**Самый простой способ:** Используйте консоль в панели Timeweb - там можно копировать и вставлять команды прямо в браузере!
