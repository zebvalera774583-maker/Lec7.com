cd ~/Lec7.com && echo "=== СТАТУС КОНТЕЙНЕРОВ ===" && docker-compose ps && echo "" && echo "=== ЛОГИ ПРИЛОЖЕНИЯ (последние 30 строк) ===" && docker-compose logs --tail=30 app && echo "" && echo "=== ПРОВЕРКА БАЗЫ ДАННЫХ ===" && docker-compose exec -T postgres pg_isready -U lec7 && echo "" && echo "=== ПРОВЕРКА ПОРТА 3000 ===" && (netstat -tlnp 2>/dev/null | grep ":3000" || ss -tlnp 2>/dev/null | grep ":3000" || echo "Порт не слушается") && echo "" && echo "=== ПРОВЕРКА ДОСТУПНОСТИ ===" && curl -s -o /dev/null -w "HTTP код: %{http_code}\n" http://localhost:3000 || echo "Приложение не отвечает"
